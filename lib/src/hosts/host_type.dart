/// Host type
enum HostType {
  /// Image host.
  ///
  /// Image host serves full size page images.
  image,
  /// Thumbnail host.
  ///
  /// Thumbnail host serves page thumbnails and covers.
  thumbnail;
}
